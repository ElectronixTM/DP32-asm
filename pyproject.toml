[project]
name = "dsp32-asm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "sly>=0.5",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
